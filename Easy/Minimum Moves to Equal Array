class Solution:
    def minMoves(self, nums: List[int]) -> int:
        
        output = 0
        
        nums = sorted(nums)
        
        for i in range(len(nums)):
            output += nums[i] - nums[0]
            
        
        return output
