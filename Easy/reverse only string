class Solution:
    def reverseOnlyLetters(self, S: str) -> str:
        
        letters = [c for c in S if c.isalpha()]
        
        ans =''
        
        for c in S:
            if c.isalpha():
                ans+=letters.pop()
                
            else:
                ans+=c
        
        return ans
