class Solution:
    def removeVowels(self, s: str) -> str:
        
        vowel = ['a','e','i','o','u']
        
        output = ''
        
        for i in s:
            if i not in vowel:
                output+=i
                
        return output
