class Solution:
    def smallestRangeI(self, A: List[int], K: int) -> int:
        
        if len(A)==1:
            return 0
        
        M = max(A)
        m = min(A)
        
        if M-m<=2*K:
            return 0
        else:
            return M-m-2*K
