class Solution:
    def countBits(self, n: int) -> List[int]:
        
        output = []
        
        for i in range(n+1):
            x = bin(i)
            output.append(x.count('1'))
        
        return output
