class Solution:
    def reverseBits(self, n: int) -> int:
        
        n = bin(n)[:1:-1]
        
        length = len(n)
        
        while length<32:
            n+='0'
            length+=1
        
        return int(n,2)
