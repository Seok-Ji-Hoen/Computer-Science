class Solution:
    def countMatches(self, items: List[List[str]], ruleKey: str, ruleValue: str) -> int:
        
        h = {'type':0,'color':1,'name':2}
        output=0
        for i in items:
            if i[h[ruleKey]]==ruleValue:
                output+=1
        
        return output
