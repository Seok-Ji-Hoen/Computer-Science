class Solution:
    def isPrefixString(self, s: str, words: List[str]) -> bool:
        
        prefix = ''
        
        for word in words:
            
            prefix += word
            
            if len(prefix)==len(s):
                
                if prefix==s:
                    return True
                else:
                    return False
        
        return False
