class Solution:
    def anagramMappings(self, A: List[int], B: List[int]) -> List[int]:
        
        output = []
        
        for i in A:
            output.append(B.index(i))
            
        return output
